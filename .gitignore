# Pixi (env dir is local)
.pixi/

# Local secrets — never commit (Atlas X.509 cert + private key, env files, etc.)
secrets/
*.pem
*.env

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Node / frontend
node_modules/
dist/
.vite/
*.local
*.tsbuildinfo

# Data & artifacts (do not commit papers or large corpora)
data/
research/sources/pdfs/
*.pdf
*.sqlite
*.duckdb
uploads/
.cache/
*.lock
# pixi.lock IS committed (reproducible envs + CI caching); the blanket *.lock is for stray lockfiles.
!pixi.lock
# Regenerated validation outputs (the fake-goldset INPUTS are committed; the harness OUTPUTS are not)
validation/_fake_reports/
validation/reports/

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp

# Secrets
secrets/
*.pem
*.env
.claude/launch.json
.claude/settings.json