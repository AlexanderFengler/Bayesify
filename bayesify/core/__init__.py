"""bayesify-core — the engine.

Pure Python, no web framework imports (enforced by the import-linter contract in pyproject.toml).
The interactive tool and the Phase-3 corpus pipeline both import this package, so one rubric, one
engine, and one scoring rule are used identically in both.
"""
